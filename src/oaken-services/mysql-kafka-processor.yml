FROM python:3.11

RUN apt update

RUN apt install pkg-config

RUN pip install kafka-python mysql-connector-python s3fs boto3

RUN mkdir -p /app

COPY app/mysql-api/mysql-api.py /app

WORKDIR /app

CMD ["python", "mysql-api.py"]
